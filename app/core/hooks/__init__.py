"""
Callback hooks module for extending agent functionality.

This module provides a flexible callback mechanism that allows for 
custom behaviors such as logging, notifications, or conditional branching
without changing the core agent logic.
"""
